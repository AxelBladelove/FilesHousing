# Task B4: Implement safe backend actions and Explorer integration

PERFORMANCE-FIRST DIRECTIVE (non-negotiable):
Every design and implementation decision optimizes runtime performance first.
- Choose the leanest viable approach: standard library before dependency, dependency
  only when it clearly beats hand-rolling on speed AND maintenance.
- Know your hot path: pick data structures and algorithms for the access pattern this
  code will actually have; state the expected complexity in a code comment only when
  a slower-looking choice is deliberate.
- No premature abstraction: layers, generics and indirection must pay for themselves.
- Measure when it matters: if the task touches a hot path, add or run a micro-benchmark
  or timing check and report the numbers in your result block.
- Budgets: fast startup, low allocation churn, minimal I/O round-trips, smallest viable
  bundle/binary. If you must trade performance for anything, say so explicitly in NOTES.

## Context

You are working in a Codex project worktree for FilesHousing after B2 has implemented real scanner output with paths. The UI already has a cleanup queue and details panel. Your slice is safe backend action plumbing, not aggressive cleanup automation.

## Requirements

1. Ensure scanner nodes carry a backend-safe path or action token for files/folders where actions are valid.
2. Implement `open_path` so it opens Windows Explorer for a file/folder path when available, with safe validation and non-Windows fallback.
3. Add preview-style cleanup queue backend support if lightweight: estimate reclaimable bytes from selected paths/tokens without deleting.
4. If implementing recycle/delete plumbing, make it opt-in, reviewable, and Recycle Bin first. It must never run automatically from the current UI.
5. Update frontend action calls only where necessary; no UI redesign.
6. Add focused tests or command-level checks for validation helpers where practical.
7. Commit your work on your worktree branch with clear messages.

## Done criteria

- `cargo check` from `src-tauri/` exits 0.
- `bun run build` exits 0.
- "Open in Explorer" is wired to backend in Tauri mode and still friendly in browser mode.
- No destructive action can happen without an explicit future confirmation path.
- Final message ends with the required RESULT block.

## Rules

- Commit your work on this branch with clear messages. Do not touch other branches.
- Effort & delegation: this task is tier medium. Delegate heavy implementation to your `aloop-med` subagent in this chat; use cheap subagents for independent tests if efficient. When ordered to review, spawn `aloop-review` in this chat.
- Run the project's tests/build before finishing; if you cannot make them pass, say so.
- End your FINAL message with exactly this block:
  RESULT: DONE | PARTIAL | BLOCKED
  SUMMARY: <3-6 lines: what was built, key decisions>
  PERF: <perf-relevant choices and any measurements>
  NOTES: <caveats, TODOs, anything the orchestrator must know>
