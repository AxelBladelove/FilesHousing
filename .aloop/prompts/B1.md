# Task B1: Scaffold Tauri 2 Rust backend and app contracts

PERFORMANCE-FIRST DIRECTIVE (non-negotiable):
Every design and implementation decision optimizes runtime performance first.
- Choose the leanest viable approach: standard library before dependency, dependency
  only when it clearly beats hand-rolling on speed AND maintenance.
- Know your hot path: pick data structures and algorithms for the access pattern this
  code will actually have; state the expected complexity in a code comment only when
  a slower-looking choice is deliberate.
- No premature abstraction: layers, generics and indirection must pay for themselves.
- Measure when it matters: if the task touches a hot path, add or run a micro-benchmark
  or timing check and report the numbers in your result block.
- Budgets: fast startup, low allocation churn, minimal I/O round-trips, smallest viable
  bundle/binary. If you must trade performance for anything, say so explicitly in NOTES.

## Context

You are working in a Codex project worktree for FilesHousing. The current app is a Vite/TypeScript UI with mock disk data:

- `package.json`: Vite scripts only.
- `src/main.ts`: renders disk selection, scan overlay, storage map, search, filters, details, cleanup queue.
- `src/mockData.ts`: mock `Disk`/`FsNode` data source.
- `src/types.ts`: frontend data contract for `Cat`, `FsNode`, `Disk`.
- `fileshousing_brief.md`: product and technical brief.

The UI is considered ready with mock data. Your slice is only the backend foundation and shared data/API contracts. Do not redesign the UI.

## Requirements

1. Add Tauri 2 app scaffolding under `src-tauri/` suitable for Windows-first desktop development.
2. Add the minimal npm dependencies/scripts required to build/run the Tauri app from this existing Vite frontend.
3. Define Rust serializable models that match the existing frontend `Disk`/`FsNode` shape, using path strings or IDs in a way that can safely cross the Tauri boundary.
4. Expose initial Tauri commands:
   - list available scan roots/drives.
   - scan a selected root/path and return a `Disk`-like tree.
   - open a path in Explorer as a stub or real implementation if cheap and safe.
5. Provide a placeholder scanner implementation that returns a tiny real or synthetic tree only until B2 replaces it. Keep the module boundaries ready for a real scanner.
6. Update TypeScript types/API wrappers only as needed for compile-time contract clarity. Preserve mock fallback ability.
7. Commit your work on your worktree branch with clear messages.

## Done criteria

- `bun install` or the current package manager lock is updated if dependencies change.
- `bun run build` exits 0.
- `cargo check` from `src-tauri/` exits 0, or if Rust/Tauri tooling is unavailable, document the exact missing tool and keep files syntactically correct.
- The app still defaults to working with mock data until B3 wires live backend calls.
- Final message ends with the required RESULT block.

## Rules

- Commit your work on this branch with clear messages. Do not touch other branches.
- Effort & delegation: this task is tier medium. Delegate heavy implementation to your `aloop-med` subagent in this chat; you may fan out additional aloop-min/low subagents for independent subtasks when it is token-efficient. When ordered to review, spawn `aloop-review` in this chat.
- Run the project's tests/build before finishing; if you cannot make them pass, say so.
- End your FINAL message with exactly this block:
  RESULT: DONE | PARTIAL | BLOCKED
  SUMMARY: <3-6 lines: what was built, key decisions>
  PERF: <perf-relevant choices and any measurements>
  NOTES: <caveats, TODOs, anything the orchestrator must know>
